apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gatus
# Service health dashboard. UI gated behind Authentik OIDC (see values.yaml).
helmCharts:
  - name: gatus
    repo: https://twin.github.io/helm-charts
    version: 1.5.0 # renovate: helm
    releaseName: gatus
    namespace: gatus
    valuesFile: values.yaml
resources:
  - secret.sops.yaml
