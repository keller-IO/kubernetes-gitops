# ENCRYPT BEFORE COMMIT:  sops --encrypt --in-place secret.sops.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: gatus-oidc
  namespace: gatus
type: Opaque
stringData:
  # OIDC client secret — must match client_secret in the Authentik gatus blueprint.
  OIDC_CLIENT_SECRET: REPLACE_ME
