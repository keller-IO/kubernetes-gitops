apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: phpmyadmin
# Stateless MySQL/MariaDB admin UI — offizielles phpMyAdmin-Image, KEIN
# Bitnami-Chart mehr (freier Bitnami-Katalog seit Broadcom-Move eingefroren,
# Images von docker.io/bitnami nicht mehr ziehbar).
resources:
  - deployment.yaml
  - ingress.yaml
