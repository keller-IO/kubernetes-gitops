apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: roundcube
# Webmail frontend only. Points at an EXTERNAL IMAP/SMTP server (no mailserver in-cluster).
resources:
  - database.yaml
  - cache.yaml
  - workload.yaml
  - backup.yaml

generators:
  - ./secret-generator.yaml
