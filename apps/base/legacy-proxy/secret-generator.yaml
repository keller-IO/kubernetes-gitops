# KSOPS-Generator fuer die vorgeladenen Cutover-Zertifikate.
# ⚠️ TEMPORAER: beide Dateien und dieser Generator fliegen wieder raus, sobald
# cert-manager nach dem Cutover per HTTP-01 uebernommen hat.
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: preload-cert-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./preload-horads.sops.yaml
  - ./preload-jitcloud.sops.yaml
