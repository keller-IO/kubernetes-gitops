apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kimai
resources:
  - ../../../base/kimai
# Cluster-specific patches go here (host already set to the main placeholder in base).
# Example — override the Ingress host for a different cluster:
# patches:
#   - target: { kind: Ingress, name: kimai }
#     patch: |
#       - op: replace
#         path: /spec/rules/0/host
#         value: kimai.example.com
