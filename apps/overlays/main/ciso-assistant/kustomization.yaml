apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ciso-assistant
# Keine Patches noetig: grc.jit.services ist intern und terminiert TLS am
# Cluster (wie matomo). Zugriffsbeschraenkung per IP-Allowlist im Ingress,
# siehe apps/base/ciso-assistant/values.yaml.
resources:
  - ../../../base/ciso-assistant
