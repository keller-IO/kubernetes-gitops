# Operator CRs without an explicit spec.imageName (CNPG) / spec.image
# (mariadb-operator). Without it the operator's own default picks the DB
# version: it isn't in Git, Renovate can't see it, and an operator bump
# silently changes the running database version. Every line here is
# unreviewed tech debt, not an accepted design — remove it once the CR pins
# an image, and don't add a new entry without a reason.
#
# That this is a real rather than a theoretical risk is visible in the cluster
# itself: on 2026-07-28 mailman-pg ran its pinned 16.6 while every unpinned
# cluster ran the operator default 17.2 — a Postgres major-version split created
# purely by omission.

# Pinned by the open PR #46 (feat/mastodon-k8s-migration), which sets a
# digest-pinned 16.14 for the migration from the existing 16.x install.
# Deliberately left alone here to keep that PR conflict-free — drop this line
# once #46 is merged.
apps/base/mastodon/database.yaml
