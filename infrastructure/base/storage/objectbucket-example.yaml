# Example: request an S3 bucket from Ceph RGW via ObjectBucketClaim.
# The current cluster has no OBC CRD/provisioner; this file is intentionally not
# included by kustomization.yaml. Apps currently use explicit external S3 credentials.
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: example-bucket
  namespace: storage
spec:
  generateBucketName: example
  storageClassName: ceph-bucket # CHANGE ME — the RGW bucket StorageClass on your Ceph
