apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: crowdsec-pg
  namespace: crowdsec
spec:
  schedule: "0 0 2 * * *" # 6-field (CNPG = seconds first): daily 02:00
  backupOwnerReference: self
  cluster:
    name: crowdsec-pg
