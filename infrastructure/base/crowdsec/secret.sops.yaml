# NOT YET ENCRYPTED — unlike the sibling secrets in this repo, this one is still a
# blueprint placeholder (no sops/age tooling was available to encrypt it in the
# environment that generated this PR). Fill in real values, then:
#   just encrypt infrastructure/base/crowdsec/secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: crowdsec-db-app
  namespace: crowdsec
stringData:
  username: crowdsec
  password: REPLACE_ME
type: kubernetes.io/basic-auth
---
apiVersion: v1
kind: Secret
metadata:
  name: crowdsec-backup-s3
  namespace: crowdsec
stringData:
  ACCESS_KEY_ID: REPLACE_ME
  SECRET_ACCESS_KEY: REPLACE_ME
  region: REPLACE_ME # Garage erzwingt Region-Match bei HeadBucket, siehe andere backup-s3 Secrets
type: Opaque
